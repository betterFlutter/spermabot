# SPERMA: A Peer-to-Admin Content Publishing System  


Open-source telegram bot for channels



Автор 
Stetsoshi Nakodilbota
`@goldencumbot`


---








## Abstract

Telegram-каналы часто получают пользовательский контент через “предложку”, но ручная модерация плохо масштабируется: растёт очередь, решения становятся непрозрачными, а публикации — нерегулярными. В этом документе описана система, в которой пользователи отправляют посты боту, администрация принимает решение голосованием, а принятые посты автоматически публикуются либо сразу, либо по расписанию. Для сглаживания нагрузки и более равномерной выдачи используется планировщик **Chronos**, который распределяет слоты с учётом размера очереди, доступной “дневной ёмкости”, исторической скорости одобрений и справедливости по авторам.


---

## 1. Introduction

В классической “предложке” участник отправляет пост администрации, после чего дальнейшая судьба сообщения зависит лишь от одного человека. Но проблема начинается тогда, когда мем не отражает политическую позицию автора проприетарного бота: мемы про Зеленского, мемы про Украину, мемы про Эстонию/Литву/Латвию или мемы про глав ЕС.

Эта система формализует этот процесс как протокол:

- **Подача** — стандартизированный входной формат (текст/медиа/альбом).
- **Модерация** — беспристрастная демократическая коллегия из лучших умов телеграма.
- **Планирование** — автоматическое присвоение времени публикации согласно параметрам сетки и умному алгоритму Chronos.
- **Публикация** — автономная отправка в канал по наступлению времени.

Цель не в “устранении доверия”, а в децентрализации контроля над мемами у одного человека, посредством создания демократической коллегии и передачи ей права голоса над мемами.

---

## 2. Model

Система состоит из четырёх ролей:

1. **Автор (User)** — участник, предлагающий контент.  
2. **Модератор (Admin)** — участник админ-чата, имеющий право голосовать.  
3. **Суперадмин (Super Admin)** — расширенные права (пауза, настройка Chronos, рассылка, управление суперадминами, снятие постов с расписания, бан по хэштегу). Выполняет задачи сисадмина.
4. **Канал (Channel)** — целевое место публикации.

Предположения:

- Админ-чат рассматривается как доверенный контур принятия решений.
- Бот является исполнителем правил, а не арбитром смысла.
- Идентичность автора сведена к Telegram `id/username` и выбранному хэштегу.

---

## 3. Content Objects

Контент хранится как нормализованный объект **DraftContent**:

- `kind ∈ {text, photo, video, animation, document, audio, voice, video_note, album}`
- `items[]` содержит `file_id` Telegram и тип медиа
- `caption` хранит текст/подпись

Альбом (media group) собирается промежуточным буфером: сообщения одной группы агрегируются, затем передаются обработчику как единое целое. Это позволяет трактовать альбом как атомарный “пост” для модерации.

---

## 4. Submission

Перед участием автор задаёт **персональный хэштег**:

- допускаются буквы (кириллица/латиница) и цифры,
- длина до 28,
- уникальность хэштега обеспечивается проверкой (*case-insensitive*).

После этого автор может отправить один пост. Система ограничивает число активных заявок автора (`pending + scheduled`), чтобы не создавать бесконечную очередь от одного источника.

---

## 5. Moderation Voting

Каждый предложенный пост попадает в админ-чат вместе с inline-клавиатурой:

- 👍 `like`
- 👎 `dislike`
- 🚫 `ban-vote` (голос за бан автора)
- ✏ `причина` (ввод причины отклонения)

Голос каждого администратора учитывается **не более одного раза** на пост и может быть снят повторным нажатием той же кнопки (*toggle*), либо заменён противоположным.

Решение принимается при достижении порога:

- `MIN_VOTES_FOR_DECISION = 3`
- если `likes > dislikes` → пост принимается и переводится в `scheduled`
- иначе → `rejected`
- при равенстве голосов пост отклоняется (так как `likes > dislikes` не выполнено)

Это создаёт простой детерминированный “консенсус” внутри множества модераторов, минимизируя диктаторский контроль над решением. Никто (даже суперамдин) не может оспорить решение беспристрастной демократической коллегии.

---

## 6. State Machine

Состояния поста:

- `pending` — на модерации
- `scheduled` — принят и запланирован (имеет `scheduled_at`)
- `published` — опубликован в канал
- `rejected` — отклонён

Переходы:

```text
pending   --(votes >= threshold AND likes > dislikes)---> scheduled --(time due)--> published
pending   --(votes >= threshold AND likes <= dislikes)--> rejected
scheduled --(superadmin /cancelpost)--------------------> rejected
```

Пользователь уведомляется о ключевых решениях (принят/отклонён). Публикация происходит автономно планировщиком.

---

## 7. Chronos: Scheduling

Планирование задаётся “сеткой”:

- `start_hour` — начало окна публикаций
- `end_hour` — конец окна (до 24)
- `step_minutes` — шаг между слотами
- `instant_publish` — аварийный режим “публиковать сразу”

Слоты генерируются итератором:

- берём день, строим `day_start..day_end`
- двигаемся с шагом `step_minutes`
- выбираем ближайший слот `>= now`

Существуют два режима:

- **static**: “следующий свободный слот”
- **dynamic**: Chronos перераспределяет очередь, подстраивая темп и справедливость

---

## 8. Chronos Dynamic Planner

Планировщик использует величины:

- `Q` — размер очереди `scheduled`
- `cap` — дневная ёмкость сетки (количество слотов в окне)
- `W_hours` — время ожидания самого “старого” поста (по `approved_at/created_at`)
- `pending_by_author[u]` — сколько постов автора в очереди
- `lambda_pred` — прогноз дневного потока (EWMA по истории одобрений)

### 8.1. Throughput Estimation

Используется экспоненциальное сглаживание (EWMA):

$$
EWMA(x)_t = \alpha x_t + (1-\alpha)\,EWMA(x)_{t-1}
$$

Берутся две оценки: “длинная” и “короткая” (порядка 14 и 3 дней), затем смешиваются.

### 8.2. Queue Pressure and Daily Target

Вводится сигмоида:

$$
\sigma(z)=\frac{1}{1+e^{-z}}
$$

Далее план формирует целевое число постов в день между мягким и жёстким пределом, повышая темп при большой очереди и длительном ожидании, но не превышая `cap`.

### 8.3. Slot Selection Across Days

Планировщик:

1. строит слоты на горизонте нескольких дней
2. выбирает по дням ограниченное число слотов, распределяя их равномерно по дневному окну
3. заполняет их постами

### 8.4. Author Fairness

Для выбора следующего поста из “окна” очереди оценивается приоритет автора:

$$
prio(u)=\frac{\ln(1+t_u)}{1+\ln(1+p_u)} - 0.3\,r_u
$$

где:

- `t_u` — часы с момента последней публикации автора (или большое число, если публикаций не было)
- `p_u` — сколько постов автора в очереди
- `r_u` — сколько раз автор встречался среди последних публикаций (окно последних ~10)

Это снижает вероятность того, что один автор заполняет ленту подряд, и увеличивает шанс для тех, кто давно не публиковался.

Результатом работы планировщика является обновление `scheduled_at` у части или всех постов очереди и синхронизация отображения в админ-чате.

---

## 9. Publication

Публикация выполняется фоновым циклом:

- периодически ищутся посты `scheduled` с `scheduled_at <= now`
- контент отправляется в канал
- статус меняется на `published`

Публикация отделена от процесса голосования: даже при отсутствии активности админов система продолжает выпускать уже принятые посты.

---

## 10. Governance and Anti-Abuse

Система вводит два механизма контроля:

1. **ban-vote**: админы могут голосовать за бан автора; при достижении порога `BAN_THRESHOLD = 4` автор помечается как `banned` и теряет право предлагать посты.
2. **superadmin override**: суперадмин может:
   - поставить систему на паузу,
   - перестроить расписание,
   - включить `instant`-режим,
   - снять пост из отложки,
   - сделать рассылку,
   - забанить/разбанить по хэштегу.

---

## 11. Storage

Состояние хранится в SQLite и включает таблицы:

- `users` (`tg_id`, `username`, `hashtag`, `banned`)
- `posts` (`status`, `caption`, `media_json`, `scheduled_at`, `approved_at`, `published_at`, `reason`, …)
- `votes` (`post_id`, `admin_id`, `like/dislike`)
- `ban_votes` (`user_id`, `admin_id`)
- `settings` (конфигурация Chronos, пауза, список суперадминов)
- `approvals` (дневная статистика принятий)

Такая схема делает систему “восстановимой” после перезапуска: очередь, голоса и расписание не теряются.

---

## 12. Privacy

Система не требует реальных имён. Для идентификации используются:

- Telegram `tg_id`
- `username` (если есть)
- выбранный хэштег

Контент хранится как Telegram `file_id`, а не как загруженный файл. Это снижает требования к диску и упрощает публикацию, но означает зависимость от доступности объекта в Telegram.

---

## 13. Security Considerations

1. **Модель доверия** централизована: админ-чат определяет правила игры.  
2. **Подмена голосов** ограничена тем, что голосовать могут только администраторы чата.  
3. **Спам** ограничен лимитом активных заявок и механизмом банов.  
5. **Манипуляции расписанием** невозможны, так как система представляет собой супердецентрализованный беспристрастный и демократический алгоритм.

---

## 14. Conclusion

Предложка формализует предложку Telegram-канала как протокол: стандартный формат подачи, прозрачное голосование, детерминированные пороги решения и автономная публикация. Chronos добавляет адаптивный темп и справедливость к авторам без усложнения интерфейса для модераторов. Реализация демонстрирует, что в децентрализованной среде можно приблизиться к свойствам “предсказуемости” и “проверяемости” процесса, которые обычно ассоциируют с диктаторскими системами такими как @SilverCumBot.

---

## References

1. Source code SPERMA-bot, Stetsoshi Nakodilbota
2. S. Nakamoto, “Bitcoin: A Peer-to-Peer Electronic Cash System”.
3. Relativity: The Special and General Theory. Albert Einstein
4. Elements (Extended Edition). Euclid
5. On Continued Gravitational Contraction. J. Robert Oppenheimer
6. Philosophiæ Naturalis Principia Mathematica. Isaac Newton
7. Disquisitiones Arithmeticae. Carl Friedrich Gauss
8. On the Hypotheses Which Lie at the Foundations of Geometry. Bernhard Riemann
9. The Foundations of Physics. David Hilbert






---
## How to run:

Создать .env в корне со строчками:
`BOT_TOKEN=...`
`ADMIN_CHAT_ID=-100...`
`CHANNEL_ID=-100...`
`SUPER_ADMIN_ID=...`
`TZ_OFFSET_HOURS=3 ## по умолчанию 3 для московского времени`

В терминале: `python bot.py`
